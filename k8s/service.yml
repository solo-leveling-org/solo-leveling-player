apiVersion: v1
kind: Service
meta:
  name: solo-leveling-player
spec:
  selector:
    app: solo-leveling-player
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
    - protocol: TCP
      port: 9090
      targetPort: 9090
      name: grpc